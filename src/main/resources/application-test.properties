# JWT 시나리오(k6 jwt-scenarios) 전용 — spring.profiles.active=test 일 때만 적용
# 운영/일반 부하 테스트와 포트 분리 (기본 8080 유지)
server.port=18080

# Access/Refresh 만료 케이스를 짧은 시간에 확인하기 위한 TTL
ticketing.jwt.access-ttl-seconds=20
ticketing.jwt.refresh-ttl-seconds=70

