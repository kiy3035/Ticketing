# 부하 테스트 — 수기(Before) vs 자동 하네스(After) 비교

> 같은 `knee-point` 시나리오를 **수기**(예전, AWS k6→앱2대→RDS)와 **자동 하네스**(`loadtest-harness/`)로
> 측정해 비교한다. 목적: **자동화가 수기 결과를 재현하는지** 검증 = 자동화 신뢰성 입증.

## ⚠️ 비교 시 방법론 정렬 (중요)
- **수기**: Run1은 JVM cold라 느려서 **분석 제외**, **Run 2·3(JVM hot) 평균** 기준. 매 Run 전 Redis flush.
  (출처: [`load-test-portfolio.md`](load-test-portfolio.md) Phase 6)
- **자동**: 동일하게 맞추려면 **`--repeat 3` + 측정 직전 앱 재시작(run1 cold) + `reset_command`(매 회차 Redis flush)**.
- 아래 "자동(스모크)"은 **단일 1회(repeat 1)** 라, 사실상 수기에서 *제외하는* **cold Run1**에 해당 → **예비 참고용**.
  정식 비교는 **자동(repeat 3, hot 2·3 평균)** 칸이 채워진 뒤에 한다.

## 핵심 지표 비교 (knee-point)

| 지표 | 수기 (Phase 6, hot 2·3·재측정) | 자동 — 스모크 1회 (예비) | 자동 — repeat 3 (hot 2·3 평균) |
|------|------------------------------|------------------------|------------------------------|
| 평균 RPS (k6) | ~2,320 ~ 2,430 /s | **1,889 /s** | _(본 측정 후 기입)_ |
| p95 지연 | ~448 ~ 537 ms | **830 ms** | _(TBD)_ |
| max 지연 | **1m 0s** (k6 timeout) | **60.1 s** (timeout) | _(TBD)_ |
| 실패율 | 2.6 ~ 4.9 % | **5.56 %** | _(TBD)_ |
| 대기열 진입 성공률 | 86 ~ 87 % | _(미집계)_ | _(TBD)_ |

> 출처(수기): 4회차 p95 494ms·err 4.90%·RPS 2,324 / 5회차 448ms·3.20%·2,428 / hot 재측정 537ms·2.59%·2,322.

## 서버측 메트릭 (자동 스모크에서 수집된 값)
| 메트릭 | max | mean | 의미 |
|--------|-----|------|------|
| `hikari_pending` (DB 풀 대기) | **174** | 25 | 풀(서버당 30, 2대=60) 받으려 대기 폭증 → **DB 커넥션 풀 병목** |
| `hikari_active` | 48 | 12 | 활성 커넥션(2대 합산) |
| `queue_waiting` | 4,755 | 2,222 | 대기열 적체 |
| `hold_conflict` | 0 | 0 | 홀드 경합 없음 |
| `lock_acquire_failures` | 0 | 0 | 락 획득 실패 없음 |
| `error_rate`(서버 5xx) | (데이터 없음) | - | 실패는 5xx가 아니라 **timeout**(클라가 60s에 끊음) — 서버는 5xx를 안 냄 |

## 1차 해석 (예비)
- **큰 그림은 일치**: max 60s timeout(동일), 에러율 ~5%대(유사 범위), RPS 자릿수(2,200+급) 비슷, knee가 고VU 구간.
- **차이의 원인 = 단일 cold run**: 스모크가 p95↑(830 vs ~480)·RPS↓(1,889 vs ~2,370)인 건, **수기에서 분석 제외하는 cold Run1**에 해당하기 때문. → 수기 방법론과 일치하는 현상(예상됨). 정식 비교는 hot 2·3 평균으로.
- **병목 진단 일치**: `hikari_pending`(DB 풀)이 병목 신호 — 수기 Phase 5의 "VU=1500 DB pending spike"와 정렬. `hold_conflict`·`lock_acquire_failures`=0 → 락/홀드 경합은 병목 아님(수기 Phase 7 정확성 결론과 충돌 없음).

## 본 측정(repeat 3) 후 채울 것
1. 위 표의 "자동 — repeat 3" 칸 (hot 2·3 평균).
2. 수기 대비 **차이율(%)** 과 **knee point VU 구간 일치 여부**(수기: VU 1,000~1,200).
3. **결론**: 자동 하네스가 수기 결과를 ±N% 내로 재현 → 자동화 신뢰성 정량 확인. (또는 차이가 크면 그 원인 분석)

> 갱신 명령: `python run.py --scenario knee-point --repeat 3` 결과의 `report.md`를 보고 이 표를 채운다.
